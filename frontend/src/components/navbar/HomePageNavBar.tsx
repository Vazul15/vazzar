import { Button } from "../ui/button";

export const HomePageNavBar = () => {
  return (
    <div className="h-16 flex items-center justify-end px-4 bg-background text-primary">
      <Button className="text-black">Login</Button>
    </div>
  );
};
