spring:
  application:
    name: radarr-backend

radarr:
  api:
    key: ${X_API_KEY}
    host: ${RADARR_HOST}
    port: ${RADARR_PORT}

qbittorrent:
  api:
    user: ${QBITTORRENT_USERNAME}
    password: ${QBITTORRENT_PASSWORD}
    host: ${QBITTORRENT_HOST}
    port: ${QBITTORRENT_PORT}
